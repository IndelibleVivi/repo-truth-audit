# Orchard Console

This product has moved beyond the original MVP. The current programme is the
complete operator console described by `CURRENT_HEAD.json`; current work should
preserve its integrated routes rather than return to scaffolding.

`CURRENT_HEAD.json` is the current product identity snapshot. Dated status
notes are historical evidence when their date is older than that snapshot.
