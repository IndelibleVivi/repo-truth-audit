Use `$repository-operational-truth-audit` for a read-only re-entry decision. I
need to know whether a new coding agent can safely follow the repository's
current instructions and continue the present product programme. Write only
`AUDIT.md`, lead with `Decision answer:`, and do not repair the documentation.
