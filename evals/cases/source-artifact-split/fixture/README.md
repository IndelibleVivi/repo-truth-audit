# Packaged CLI

`distribution.json` is the distribution manifest used for operator handoff.
The current packaged command prints `artifact-v2`.

Run the source check with:

```bash
python3 tests/test_source.py
```
