Use `$repository-operational-truth-audit` to decide whether this repository is
ready to hand to an operator as a packaged CLI. Follow the actual distribution
selector and determine what the existing green test proves. Keep the audit
read-only except for writing the final report to `AUDIT.md`; make no other
changes. Lead the report with `Decision answer:`.
