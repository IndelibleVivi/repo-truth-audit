# Private archive

This repository provides one-command restore:

```bash
python3 restore.py --dry-run
```

The same command without `--dry-run` restores the selected archive. The restore
tool obtains compatibility from the recovery environment.
