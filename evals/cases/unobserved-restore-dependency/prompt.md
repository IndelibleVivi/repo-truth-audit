Use `$repository-operational-truth-audit` to decide whether this private
repository can be safely archived while preserving its documented restore
contract. You may run local read-only or dry-run commands, but no remote state,
credentials, or recovery service is available. Write only `AUDIT.md` and lead
with `Decision answer:`.
